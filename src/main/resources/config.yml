# You should have a world for each map that is prefixed with 'bunkers_map_' AND contains a 'map-data.yml' file
# Example world name: 'bunkers_map_Classic'
maps:
  - Classic
  - Winter
settings:
  # CAPTURE, POINTS
  win-condition: "CAPTURE"
  use-dynamic-motd: true
database:
  mongo:
    host: "127.0.0.1"
    port: 27017
    database: "bunkers"
    authentication:
      enabled: false
      username: "admin"
      password: "dev"
      database: "admin"